# Python
__pycache__/
*.py[cod]
.venv/
backend/data/*.db
# Настройки прибора — его собственные, а не исходник: правятся из админки на
# каждом экземпляре, и в репозитории давали конфликт при каждом пуле. Образец
# с подобранными значениями лежит рядом: settings.example.json.
backend/data/settings.json
# Пресеты, наоборот, общие для всех приборов — библиотека предустановок,
# выбирается в шапке настроек. В git осознанно, несмотря на тот же риск
# конфликта при пуле, что и у settings.json.
backend/data/labels/
backend/data/mock/
backend/data/photos/
# Отметки «когда прибор последний раз что-то принял» — состояние, не исходник
backend/data/state.json
backend/.env

# Node
node_modules/
frontend/dist/

# Снимки экрана и образцы, которыми объясняют задачу: место им на диске, а не в
# репозитории — читать их потом всё равно некому.
scrins/

# ОС
.DS_Store
Thumbs.db

# Claude Code: указатель и скиллы живут локально, в репозитории им не место
# CLAUDE.md
# CLAUDE.local.md
# .claude/
# 
